[package]
name = "tycl_parser"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
tycl_macro = { path = "../tycl_macro" }
logos = "0.15"
thiserror = "2"
regex = "1"
serde = { version = "1", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
